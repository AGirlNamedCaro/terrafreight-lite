<%= turbo_stream_from "shipments" %>

<div class="max-w-4xl mx-auto p-6">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold text-gray-900">TerraFreight Lite</h1>
    <%= link_to "New Shipment", new_shipment_path,
                class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded font-medium" %>
  </div>

  <div id="shipments">
    <%= render @shipments %>
  </div>
</div>
